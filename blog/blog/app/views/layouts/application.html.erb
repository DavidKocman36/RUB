<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
      <nav>
        <ul>
          <li><%= button_to "Home", root_path, method: :get %></li>
          <li><%= button_to "Blog", articles_path, method: :get %></li>
          <% unless user_signed_in? %>
            <li><%= button_to "Log in", new_user_session_path, method: :get%></li>
            <li><%= button_to "New? Sign up", new_user_registration_path, method: :get %></li>
          <% else %>
            <% if current_user.admin? %>
              <p> ADMIN </p>
            <%end%>
            <li><%= button_to "Log out", destroy_user_session_path, method: :delete %></li>
            <li><%= button_to "Profile", edit_user_registration_path, method: :get %></li>
          <% end %>
          
        </ul>
      </nav>
      <%= yield %>
  </body>
</html>
